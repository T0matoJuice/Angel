现在请根据刚才从训练数据中学习到的判断规律，对下面的测试数据进行工单性质判断。

**核心要求：严格按照训练数据的标注规律进行判断，而不是简单的关键词匹配！**
**将“旧件名称/新件名称”的差异作为重要特征：若存在明确更换行为，应提升质量怀疑权重**

测试数据（共{test_row_count}行）：
{test_content}

判断任务：
1. **回顾训练数据规律**：
   - 回忆刚才分析的训练数据中，质量工单和非质量工单的标注特征
   - 注意训练数据中相似案例的标注结果
   - 理解训练数据中各字段的判断权重和优先级

2. **逐行分析**：对每一条测试数据，按照以下步骤判断：
   - 第一步：在训练数据中寻找最相似的案例
   - 第二步：参考相似案例的标注结果
- 第三步：查看"服务项目或故障现象"字段（最重要的判断依据）
- 第四步：查看"旧件名称"与"新件名称"（若不同且均非空，提升质量怀疑权重）
- 第五步：查看"判定依据"字段（需结构化推理链）
- 第六步：综合其他字段信息（来电内容、现场诊断、处理方案）
   - 第六步：基于训练数据的标注逻辑做出最终判断

3. **判断原则**（从训练数据中学到的规律）：

   **质量工单的核心特征**（参考训练数据的标注）：
   - 服务项目明确标注为"质量鉴定"、"产品质量鉴定"
   - 判定依据指向产品本身的质量缺陷（零部件损坏、失效、性能不达标）
   - 问题根源是产品制造、设计、来料问题
   - 需要更换零部件且原因是产品质量问题（非人为、非环境）
   - 参考训练数据中类似故障现象的标注结果

   **非质量工单的核心特征**（参考训练数据的标注）：
   - 服务项目明确标注为"安装"、"调试"、"维护"、"加装"、"改装"
   - 判定依据指向安装服务、环境因素、使用不当
   - 问题根源是外部环境（水压、水质、电压）或人为因素
   - 正常的维护保养、滤芯到期更换（非质量问题）
   - 参考训练数据中类似服务类型的标注结果

4. **边界情况处理**（参考训练数据的标注方式）：
   - 如果同时涉及多个因素，参考训练数据中类似复杂案例的标注倾向
   - 如果信息不足，参考训练数据中信息不足案例的标注分布
   - 不要简单地默认为某一类，而是基于训练数据的整体规律
   - 理解问题的本质，而不是仅依赖表面关键词

5. **输出要求**：
- 必须输出完整的{test_row_count}行数据，一行都不能少
- 每行必须包含完整的13个字段
- **每一行都必须填写"工单性质"字段，绝对不能留空**
- 工单性质只能是"质量工单"或"非质量工单"
- 当判定为"非质量工单"时，"判定依据"必须包含：具体排除的质量问题类型、关键判断因素、相关训练案例参考、置信度评分（0-100%）

6. **输出格式**：
- 第一行：CSV表头（13个字段）
- 后续行：数据行（每行13个字段）
- 字段顺序：工单单号,判定依据,故障部位名称,故障组,故障类别,服务项目或故障现象,故障件简称,旧件名称,新件名称,来电内容,现场诊断故障现象,处理方案简述或备注,工单性质

**CSV格式要求**：
- 如果字段内容包含逗号(,)、引号(")或换行符，必须用双引号包裹该字段
- 字段内的双引号需要转义为两个双引号("")
- 每行数据必须包含完整的13个字段，不能缺少任何字段，不能多出任何字段
- 不要在CSV中插入空行或分隔线
- 字段内容中的换行符应该保留在引号内，不要拆分成多行

**输出示例**：
工单单号,判定依据,故障部位名称,故障组,故障类别,服务项目或故障现象,故障件简称,旧件名称,新件名称,来电内容,现场诊断故障现象,处理方案简述或备注,工单性质
WO0018061999,声音大现场检测正常,"电源适配器 J1205-ROB8C,CCC",中央净水机,结构故障,产品质量鉴定,布水器,旧J1205,新J1205,"软水机有胶状物",出水嘴故障,"布水器坏，更换",质量工单
WO0018091779,滤芯堵塞不出水,滤芯,净水机,滤芯类,线上销售产品质量鉴定,复合滤芯,旧RO,新RO,"客户留言:水里有脏东西",制水故障,漏炭更换,质量工单
WO0018103417,异响增加减压阀,前面板,饮水机,结构故障,安全维护,前面板,,,"客户留言:面板碎了",nan,面板破裂,非质量工单
WO0018103418,安装位置不合适需要移机,,净水机,安装服务,安装调试,,旧位,新位,客户要求移机位置,重新安装,移机到新位置,非质量工单

**重要提醒**：
- 请严格按照训练数据中学到的标注规律进行判断
- 不要使用简单的关键词匹配，而是理解问题的本质
- 参考训练数据中相似案例的标注结果
- 每一行的"工单性质"列都必须填写，不能为空
- 输出的数据行数必须等于{test_row_count}行
- 仅输出CSV格式数据，不要添加任何解释或推理过程
现在请根据刚才从训练数据中学习到的判断规律，对下面的测试数据进行工单性质判断。

**核心要求：严格按照训练数据的标注规律进行判断，而不是简单的关键词匹配！**

测试数据（共{test_row_count}行）：
{test_content}

判断任务：
1. **回顾训练数据规律**：
   - 回忆刚才分析的训练数据中，质量工单和非质量工单的标注特征
   - 注意训练数据中相似案例的标注结果
   - 理解训练数据中各字段的判断权重和优先级

2. **逐行分析**：对每一条测试数据，按照以下步骤判断：
   - 第一步：在训练数据中寻找最相似的案例
   - 第二步：参考相似案例的标注结果
   - 第三步：查看"服务项目或故障现象"字段（最重要的判断依据）
   - 第四步：查看"判定依据"字段（明确说明问题原因）
   - 第五步：综合其他字段信息（来电内容、现场诊断、处理方案）
   - 第六步：基于训练数据的标注逻辑做出最终判断

3. **判断原则**（从训练数据中学到的规律）：

   **质量工单的核心特征**（参考训练数据的标注）：
   - 服务项目明确标注为"质量鉴定"、"产品质量鉴定"
   - 判定依据指向产品本身的质量缺陷（零部件损坏、失效、性能不达标）
   - 问题根源是产品制造、设计、来料问题
   - 需要更换零部件且原因是产品质量问题（非人为、非环境）
   - 参考训练数据中类似故障现象的标注结果

   **非质量工单的核心特征**（参考训练数据的标注）：
   - 服务项目明确标注为"安装"、"调试"、"维护"、"加装"、"改装"
   - 判定依据指向安装服务、环境因素、使用不当
   - 问题根源是外部环境（水压、水质、电压）或人为因素
   - 正常的维护保养、滤芯到期更换（非质量问题）
   - 参考训练数据中类似服务类型的标注结果

4. **边界情况处理**（参考训练数据的标注方式）：
   - 如果同时涉及多个因素，参考训练数据中类似复杂案例的标注倾向
   - 如果信息不足，参考训练数据中信息不足案例的标注分布
   - 不要简单地默认为某一类，而是基于训练数据的整体规律
   - 理解问题的本质，而不是仅依赖表面关键词

5. **输出要求**：
   - 必须输出完整的{test_row_count}行数据，一行都不能少
   - 每行必须包含完整的11个字段
   - **每一行都必须填写"工单性质"字段，绝对不能留空**
   - 工单性质只能是"质量工单"或"非质量工单"

6. **输出格式**：
   - 第一行：CSV表头（11个字段）
   - 后续行：数据行（每行11个字段）
   - 字段顺序：工单单号,判定依据,故障部位名称,故障组,故障类别,服务项目或故障现象,故障件简称,来电内容,现场诊断故障现象,处理方案简述或备注,工单性质

**CSV格式要求**：
- 如果字段内容包含逗号(,)、引号(")或换行符，必须用双引号包裹该字段
- 字段内的双引号需要转义为两个双引号("")
- 每行数据必须包含完整的11个字段，不能缺少任何字段，不能多出任何字段
- 不要在CSV中插入空行或分隔线
- 字段内容中的换行符应该保留在引号内，不要拆分成多行

**输出示例**：
工单单号,判定依据,故障部位名称,故障组,故障类别,服务项目或故障现象,故障件简称,来电内容,现场诊断故障现象,处理方案简述或备注,工单性质
WO0018061999,声音大现场检测正常,"电源适配器 J1205-ROB8C,CCC",中央净水机,结构故障,产品质量鉴定,布水器,"软水机有胶状物",出水嘴故障,"布水器坏，更换",质量工单
WO0018091779,滤芯堵塞不出水,滤芯,净水机,滤芯类,线上销售产品质量鉴定,复合滤芯,"客户留言:水里有脏东西",制水故障,漏炭更换,质量工单
WO0018103417,异响增加减压阀,前面板,饮水机,结构故障,安全维护,前面板,"客户留言:面板碎了",nan,面板破裂,非质量工单
WO0018103418,安装位置不合适需要移机,,净水机,安装服务,安装调试,,客户要求移机位置,重新安装,移机到新位置,非质量工单

**重要提醒**：
- 请严格按照训练数据中学到的标注规律进行判断
- 不要使用简单的关键词匹配，而是理解问题的本质
- 参考训练数据中相似案例的标注结果
- 每一行的"工单性质"列都必须填写，不能为空
- 输出的数据行数必须等于{test_row_count}行
- 仅输出CSV格式数据，不要添加任何解释或推理过程